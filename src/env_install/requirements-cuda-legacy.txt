-r requirements-pip-options.txt
--extra-index-url https://download.pytorch.org/whl/cu128
--extra-index-url https://pypi.nvidia.com
torch==2.7.1
torchvision==0.22.1
# torchaudio==2.7.1

# Optional NVIDIA zero-copy encoder API; FFmpeg remains the fallback.
PyNvVideoCodec==2.2.0; platform_system == "Windows" and platform_machine == "AMD64"

# Optional Triton dependency for torch.compile on Windows.
triton>=3.6,<3.7; sys_platform == "linux"
triton-windows==3.6.0.post26; sys_platform == "win32"
wc-cuda==0.1.3; sys_platform == "win32"

# Optional TensorRT Python packages already present in this environment.
tensorrt_cu12==10.14.1.48.post1
tensorrt_cu12_bindings==10.14.1.48.post1
tensorrt_cu12_libs==10.14.1.48.post1
